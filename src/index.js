import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//this is the only time in our app that we are using the DOM API

ReactDOM.render(<App />, document.getElementById('root'));